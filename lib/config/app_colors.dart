import 'dart:ui';

abstract final class AppColors {
  // Backgrounds & Surfaces
  static const deepSpace = Color(0xFF0B0C10);
  static const stardust = Color(0xCC1F2833); // #1F2833 at 80% opacity

  // Gameplay & UI Accents
  static const sphereCyan = Color(0xFF00E5FF);
  static const apexMagenta = Color(0xFFFF007F);
  static const blockYellow = Color(0xFFFFD600);
  static const starBitWhite = Color(0xFFFFFFFF);
  static const failCrash = Color(0xFFFF3D00);
}
